# UI Spec

## MODIFIED Requirements

### Sidebar Toggle
The application MUST display exactly one sidebar toggle button (hamburger menu) in the header area.

#### Scenario: Viewing Header
Given the user is on any page with the sidebar
When they look at the header
Then they see only one hamburger menu icon
And clicking it toggles the sidebar
